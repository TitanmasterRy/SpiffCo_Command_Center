# Plugins

Drop-in plugin directory. The plugin system (backend entry points + frontend
panel registration) is specified in `docs/PLUGIN_GUIDE.md` and implemented
after the core phases stabilize the internal APIs it must expose.
