# JSON Schemas

JSON Schema documents validating the files in `../../database/data/`.
Field definitions are documented in `docs/DATABASE_SCHEMA.md`; formal schema
files are added together with the full game-data import script.
