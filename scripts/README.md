# Scripts

Development and maintenance scripts.

| Script | Purpose |
|---|---|
| `dev.ps1` | Start backend + frontend dev servers (Windows) |
| `dev.sh` | Start backend + frontend dev servers (POSIX) |
| `import_game_data.py` | (Planned) Import full game data from the game's Docs.json |
